# Chezmoi
alias cm="chezmoi"

# General aliases
alias git='LANG=en_GB.UTF-8 git'
{{- if eq .chezmoi.os "darwin" }}
alias ll='ls -lAhFGW'
{{- else if eq .chezmoi.os "linux" }}
alias ll='ls -lAhFG'
{{- end }}
alias nv='nvim'

# Dev specifics
alias tmp='cd ~/dev/tmp/'
alias dev='cd ~/dev/'

# Yarn
alias yb='yarn build'
alias yd='yarn dev'
alias yw='yarn watch'
alias ys='yarn start'
alias ysd='yarn start:dev'
alias ysp='yarn start:prod'

# NPM
alias ns='npm run start'
alias nsd='npm run start:dev'
alias nsp='npm run start:prod'
alias nb='npm run build'

# DevOps tools
alias kb='kubectl'
alias kc='kubectx'
alias tf='terraform'
alias dc='docker-compose'

# Personal
[ -f ~/.aliases/personal.sh ] && source ~/.aliases/personal.sh

{{ if eq .chezmoi.os "darwin" -}}
# Darwin
[ -f ~/.aliases/darwin.sh ] && source ~/.aliases/darwin.sh
{{- end }}

{{- if eq .hostname "pc-vivacity-pl" "blastoise" }}
# Vivacity
[ -f ~/.aliases/vivacity.sh ] && source ~/.aliases/vivacity.sh
{{- end }}

{{- if eq .hostname "pc-vivacity-pl" "blastoise" }}
# MyFoodCourt
[ -f ~/.aliases/mfc.sh ] && source ~/.aliases/mfc.sh
{{- end }}
